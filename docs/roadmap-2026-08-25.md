# Roadmap: single-voice + память + UX (25.08.2026)

Сводный план реализации всех решений этой недели: single-voice архитектура,
`memory-design.md` (rev 2.1), план фиксов Опуса, остатки UX-аудита (~80 находок).

**Модель исполнения:** каждый пакет — Opus-субагент (reasoning effort xhigh) в
изолированном worktree; после пакета — code-review (уровень high) вторым
агентом + прогон тестов; мерж — по зелёному ревью. Пакеты внутри фазы, не
трогающие общие файлы, идут параллельно.

**Три принципа порядка:**
1. **Не полировать умирающее.** Треть UX-находок (рендер результатов воркера,
   якорные редактирования, фокус-тексты, тексты /stop) живёт в путях, которые
   single-voice удаляет. Сначала переделка, потом полировка того, что осталось.
2. **Схема и фундамент раньше потребителей.** owner.timezone, расширение
   daemon_events, процесс-хардening — предпосылки почти всего.
3. **Один несущий пакет за раз.** Фазы 1 и 2 меняют operator-daemon.ts в одних
   и тех же местах (конверт, очередь) — их пакеты последовательны; всё
   остальное параллелится.

---

## Фаза 0 — фундамент (все пакеты независимы, параллельно)

| Пакет | Содержимое | Источник |
|---|---|---|
| 0.1 Процесс | `uncaughtException`/`unhandledRejection` (лог + clean_shutdown + exit), дедлайн на stop() с записью маркера, `SIGINT` через `on` (второй сигнал форсирует), откат на дефолтный провайдер вместо отказа бута | план Опуса №8 |
| 0.2 Event-лог | усечённые аргументы (≤500) и результат/ошибка (≤300) мутирующих вызовов в `daemon_events` + привязка opturn + **redaction-at-write payload_json тем же коммитом** | memory-design §2.4/§8.1 |
| 0.3 Конфиг | `owner.timezone` (IANA) в zod-схему и конфиг; helper локального времени/границы суток 03:00 | memory-design §2.7 |
| 0.4 Окружение | белый список env для потомков (PATH/HOME/LANG/TZ/ANTHROPIC_*/CLAUDE_*/OPENAI_*/BASH_*) + `OPERATOR_ENV_PASSTHROUGH` | план Опуса №2 |
| 0.5 Фенс | `fenceUntrusted` на `web_search`, `email.search`, `calendar.list_events`, `artifacts.read_text`, worker `agent_message` (метка `tool`) | план Опуса №1 |
| 0.6 Approvals | TTL (часы, не минуты — владелец спит), активное истечение в тике (снять клавиатуру, отредактировать в «запрос истёк»), потолок 4 pending на чат, фикс спиннера (answerCallback до completeEvent) | план Опуса №5 |
| 0.7 Доставка-мин | уведомление после ~10 ретраев (продолжая ретраить), прямая отправка при забитой голове очереди (минуя outbox, троттлинг 60 с) | план Опуса №6 |

DoD фазы: тесты зелёные, ни одного изменения в конверте/очереди (не конфликтует
с фазой 1).

### Отложенные issue фазы 0 (из ревью, не блокировали мерж)

- 0.2: ключи `sshkey|signingkey|encryptionkey|secretkey` добавить в суффиксный
  матч точечно (значенческие правила пока страхуют); тихий catch в
  `boundedJsonText` превращает циклический результат тула в "[object Object]"
  без теста; `appendEvent` бросает на циклическом payload внутри try тула;
  pino-redact — третья копия списка секретных ключей (TODO стоит).
- 0.3 → пакет 2.1: `utility.time` укрепить через `resolveTimeZone(tz, fallback)`
  (единственный неукреплённый недоверенный call-site); тест UTC-дефолта
  automations закрепить явным `TZ`.
- 0.4: закрыто (tripwire-тест добавлен при мерже, c49d8e9).
- 0.5: `summary.files` фенсить меткой worker (обоснование «пути валидируются»
  неверно — это свободные строки из LLM-парсинга, валидация происходит при
  использовании, а модель читает до); комментарий о границе достижимости
  FIFO нонсов (512); дефанг портит легитимные `<<<…>>>`-шаблоны (CUDA) в
  читаемых файлах — принято как компромисс; label-тест проверяет подстроку,
  не семантику.
- 0.6: карточка на успешном пути не показывает исход (только снимает
  клавиатуру — дописать «Разрешено/Отклонено» через готовый closeApprovalCard);
  `deleteRuntimeState` + чистка ключей `approval_expiry_attempts:*` и
  `approval_requested_at:*` (текут); `findPendingApprovalByT3` не видит
  claim-статусы → переэмиссия в 15-с окне заведёт вторую карточку; переименовать
  тест гонки потолка под реальный ассерт.
- Внесено при мерже мной: 0.6 lease покрывает `deciding` + порог предохранителя
  5→30; 0.7 дедуп-ключ паузы по моменту (updatedAt), не по счётчику; 0.5
  композиция truncateFenceAware×safeSlice (опциональный cutter).
- Общее: сводный док-проход по dialogue-flow выполнен при мерже фазы (§5, §9,
  §11, §13, §16, §19).

## Фаза 1 — single-voice ядро (последовательно, это несущая переделка)

| Пакет | Содержимое |
|---|---|
| 1.1 Полосы и вытеснение | приоритетные полосы в очереди оператора (user > thread-events > background); каждое сообщение пользователя вытесняет идущий ход (`runtime.interrupt` + пере-диспатч); дайджест-батчинг событий тредов (не будить оркестратора на каждый прогресс) |
| 1.2 Единственный голос | события тредов (progress, agent_message, результат, терминал) → синтетические системные сообщения оркестратору; прямая доставка в чат и шаблонные рендеры демона удаляются; **деградированный fallback**: ход не прошёл за N минут / провайдер лежит → короткое шаблонное уведомление («работа X завершена, подробности когда восстановлюсь») |
| 1.3 Команды и фокус | удалить `/stop`, `/cancel`, `/focus`, `memory.update_focus`; сохранить cancel-word люк (пути A и B детерминированно); focus_state → внутренняя привязка; тексты про фокус из всех ответов |
| 1.4 Reply-контекст | `update.reply` (цитата) передаётся в конверт как данные; исходящие финалы и `telegram.send_*` привязываются threadId; reply на любое сообщение бота маршрутизируется агентом осмысленно |
| 1.5 Watchdog | зависший ход: ждущие в очереди + нет событий N сек → interrupt, grace, освобождение очереди; зависший тред: терминальный таймаут с уведомлением |

DoD: пользователь получает ЛЮБОЙ контент только из хода оркестратора; вытеснение
работает; e2e-runbook сценарии проходят.

### Долг пакета 1.2 (принято осознанно, чинить в фазе 2/1.x)

- **TODO 2.1/2.2 — структурное извлечение результата работы.** Вместе с
  `normalizeWorkerResult` исчез единственный источник `importantDecisions`,
  `changedFiles`, `unresolved`, `suggestedNextActions` в `thread_summaries`:
  теперь там только `currentState` = усечённый отчёт воркера. Мост до фазы 2 —
  строка в policy-промпте («после завершения работы зафиксируй решения/файлы
  через `memory.remember`»), закреплённая тестом. Восстановить структуру
  правильным способом: не отдельным LLM-проходом демона, а инструментами
  памяти внутри хода оркестратора (2.1 — рендеры слоёв, 2.2 — journal/now).
- **Прямые пути демона, оставшиеся вне голоса оркестратора** (нарушают букву
  DoD «пользователь получает ЛЮБОЙ контент только из хода оркестратора»,
  оставлены осознанно, каждый — отдельная задача):
  - доставка запрошенных артефактов (файлы): у оркестратора нет тула отправки
    документа — убрать путь можно только вместе с `telegram.send_document`;
  - уведомление «тред продолжили напрямую в T3» (external turn);
  - «не удалось отправить отложенное уточнение после нескольких попыток»
    (`followup_failed`) и give-up обычного ingress;
  - ack диспатча `worker_started` из `t3.send_turn`/`t3_dispatch`.
- Фолбэк-эпоха: `voice_pending_terminal:` и `voice_relaying:` — единственные
  ключи, которые фаза 2 не должна переиспользовать под другие смыслы.

## Фаза 2 — память, пакеты 1–2 (последовательно после фазы 1: тот же конверт)

| Пакет | Содержимое (memory-design §8.1–8.2) |
|---|---|
| 2.1 Push-каркас | классификатор пауз (+таблица маппинга), рендеры трёх слоёв в policy с юнитами на бюджеты, снапшот/дифф в answerDirect + компакт-восстановлении + fresh-session-replay, персистентный baseline в runtime_state, временный индекс легаси-заметок, `memory.get`, персона: нумерованные правила + расцепление persona/policy + выжимка в компакт |
| 2.2 Now-state | таблица (source/thread_ref/origin_job), `now.update`/`now.get` с идемпотентностью, демоновские авто-пункты (пункт на тред), per-item линтер с фиксированными фидбеками, ранжирующий рендер, минимальная journal_entries (автозаписи закрытий), проверка в моменте (только now, ≤2 напоминаний, внутри эпизода) |

## Фаза 3 — память 3–5 + автоматизации (3.1 раньше 3.2; 3.3 параллельно им)

| Пакет | Содержимое |
|---|---|
| 3.1 Секретарь и журнал | oneShot через Claude-ветку независимо от провайдера, has_work-гейт, сверка event-лог↔журнал↔now, TTL-переносы, дневные сводки, месячный rollup из journal_entries, `journal.*`, алерт после 3 пропусков |
| 3.2 Заметки-2 | key/description(триггер-форма)/verified_at/valid_until/superseded_by, транзакция supersede + ревизия фильтров + чистка векторов, MiniLM + дедуп на письме, дистилляция в секретаре, судьба maintainStructuredMemory, display-redaction по перечню точек, маскирование вместо уничтожения |
| 3.3 Три аппы | `automations.kind='reminder'`/`rrule`/`escalate`, `automation.update`, эскалация по подтверждению (now-пункт waiting), human-даты в owner.timezone two-layer, `calendar.*`, синтетика «system message from calendar» |

## Фаза 4 — отзывчивость и доставка (параллельно, после 1: пути стабильны)

| Пакет | Содержимое | Источник аудита |
|---|---|---|
| 4.1 Живость | heartbeat не глушит typing (previewTouched после writer), max-wait 700–1000 мс в DraftWriter, typing во время STT/скачивания + промежуточное «расшифровываю…» для длинных медиа, ack батча до скачивания | латентность №1–5 |
| 4.2 Рендеринг | `<details>`→легаси конверсия, одинарный курсив, таблицы в `<pre>`, экранирование заголовков везде, языковая чистка всех строк (RU-only, включая callback-тосты и «Категория риска»), человеческие статусы вместо снейк-кейса | тексты №1–17 |
| 4.3 Команды | setMyCommands, `enqueueBatchRemainder` для командного пути (батч не съедает сообщения), неизвестная команда → подсказка, пагинация списков, /help по ролям, /share через resolveProjectReference | команды №1–6, 13–15 |
| 4.4 Очередь/ретраи | reliability-pump не блокируется за ходом, таймаут на T3-fetch в командах (локальный ответ первым), agent_message-троттлинг | латентность №10–14 |

## Фаза 5 — гигиена и полировка (после 3; низкий приоритет)

| Пакет | Содержимое |
|---|---|
| 5.1 Библиотекарь и ретро | чеклист для SQLite-носителя (отдельный документ), скоринг свежести, history находок, ретро персоны на метриках, перепроверка valid_until | 
| 5.2 Кнопки и вопросы | след выбора в истории (редактирование текста вопроса), «Назад» в многовопросном вводе, сообщения вместо тостов для инструкций, лейблы 60 симв. |
| 5.3 Медиа и остатки | caption 4096 через доп. сообщение, MB/MiB единообразие, эмодзи-система статусов, остальные minor из аудита |

---

## Диаграмма зависимостей

```
Фаза 0 (7 пакетов параллельно)
   └─→ Фаза 1 (1.1 → 1.2 → 1.3 → 1.4 → 1.5 последовательно)
          ├─→ Фаза 2 (2.1 → 2.2)
          │      └─→ Фаза 3 (3.1 → 3.2; 3.3 ∥)
          │             └─→ Фаза 5
          └─→ Фаза 4 (4.1–4.4 параллельно, независимы от 2/3)
```

Критический путь: 0.2/0.3 → 1.1 → 1.2 → 2.1 → 2.2 → 3.1 → 3.2.
Фаза 4 — «карман» параллельной работы на всё время фаз 2–3.

## Правила для субагентов-исполнителей

- Один пакет = один worktree = одна ветка `track/<номер>-<slug>`.
- Перед кодом — прочитать `dialogue-flow.md` (поведение), `memory-design.md`
  (дизайн), релевантные находки аудита; план в начале работы, тесты на новые
  контракты (бюджеты рендеров, идемпотентность, классификатор пауз).
- Ничего не показывать пользователю as-is из тредов — проверять на каждом
  изменении доставки (правило single-voice).
- После пакета: самопроверка по DoD фазы + code-review high вторым агентом +
  полный прогон тестов; находки ревью чинятся до мержа.
- Пакеты фаз 1 и 2 не параллелить между собой (общие файлы: operator-daemon
  конверт/очередь, policy, storage).

### Отложенные issue пакета 1.1 (low, из реверификации)

- Дайджест: `completion → agent_message → progress` снова открывает тред для
  прогресса (редкая форма).
- Реплей вытесненной джобы, которая всё же ответила, не снимает свою
  chat_pending-запись — следующий ход получит лишнее «не диспетчеризуй снова».
- Батчинг транспорта ключуется только chatId — в форуме сообщения двух топиков
  могут склеиться в один ход (ложных вытеснений нет; тема форумной поддержки).
- Правка сообщения, чей ход ещё не стартовал, может дать два хода (унаследовано
  от main).
